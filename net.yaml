apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-outbound-db-access
spec:
  podSelector:
    matchLabels:
      app: spring-boot-app
  egress:
  - to:
    - podSelector:
        matchLabels:
          app: postgres
